import api from "@/app/services/api";

export const getProfiles = async () => {
   
   /*const response = await api.get('/api/profile/');
   console.log('msdlçmçmç');
   console.log(response.data);
    return response.data.map((profile) => ({ 
        value: profile.id,
        label: profile.name,
    }));*/
  
    
}