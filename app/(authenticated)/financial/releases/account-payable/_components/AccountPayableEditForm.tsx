import { AccountPayableEditProps } from '../types';

type Props = {
  data: AccountPayableEditProps;
  resetAccountPayableStore: () => void;
};

export default function AccountPayableEditForm({ data }: Props) {
  return <></>;
}
